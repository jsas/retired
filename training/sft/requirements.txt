# TRL SFTTrainer + eval dependencies. The pre-train corpus is generated with
# the repo's own `npx tsx training/generate.ts`; this wraps the HF-side run.
# Match to a specific torch-bin that your GPU driver accepts (cu12.6+ for bf16).
torch>=2.9,<3
transformers>=4.45,<4.52
trl>=0.12,<0.15
datasets>=2.20,<3
accelerate>=0.33,<1
# peft/bitsandbytes only useful for the QLoRA fallback — see SPIKE.md.
# Comment them in if you go QLoRA, otherwise skip.
# peft>=0.12,<0.16
# bitsandbytes>=0.43,<0.46
tensorboard
# chat template / tokenizer extras (Qwen uses a chat template if provided)
sentencepiece
tiktoken
