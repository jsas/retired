# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-single
dist-ssr
*.local
# Local secrets for the dev markup overlay (model endpoint/key) — never commit.
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
src/test/tmp/

# Claude Code worktree scratch (nested git repos — never commit)
.claude/worktrees/

# Elective probe: headless Chrome profile scratch + sweep output (never commit),
# except the curated fine-tune baselines (issue #127 — pre-training reference
# grids that checkpoints get diffed against).
.probe-chrome/
probe/results/*
!probe/results/baseline-*.json

# #112 spike: generated corpus artifacts. The engine is deterministic, so
# `npx tsx training/generate.ts` reproduces these; don't commit the JSONL.
training/data/
