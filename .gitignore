# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-single
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
src/test/tmp/

# Claude Code worktree scratch (nested git repos — never commit)
.claude/worktrees/

# Elective probe: headless Chrome profile scratch + sweep output (never commit),
# except the curated fine-tune baselines (issue #127 — pre-training reference
# grids that checkpoints get diffed against).
.probe-chrome/
probe/results/*
!probe/results/baseline-*.json

# #112 spike: generated corpus artifacts. The engine is deterministic, so
# `npx tsx training/generate.ts` reproduces these; don't commit the JSONL.
training/data/

# #135 fine-tune: trainer output, vendored llama.cpp converter, GGUF quants.
# All reproducible: train.py regenerates out*/ (out/, out-v3/, out-v4/, …),
# fetch_llama_cpp.py re-vendors, convert_hf_to_gguf.py re-quantizes.
# Checkpoints/quants are far too big for git.
training/sft/out*/
training/sft/llama.cpp/
training/sft/quantized/
training/sft/__pycache__/
training/driver/evalset.json
